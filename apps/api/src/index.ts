/**
 * Standalone HTTP API (Fastify + tRPC external surface per architecture).
 * Scaffolded in Phase 0; full service lands in roadmap Phase 1 (platform integration).
 */
export const API_STUB_VERSION = "0.0.0";
